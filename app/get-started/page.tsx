"use client";

import GetStartedClient from "./GetStartedClient";

/**
 * Get started: questionnaire first (role multi-select + tailored questions),
 * then consumer plans or role-based redirect. Completed users see plans directly.
 */
export default function GetStartedPage() {
  return <GetStartedClient />;
}
